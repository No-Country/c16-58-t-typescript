// /** @type {import('next').NextConfig} */
// const nextConfig = {

//     outDir: 'build',
// };

// export default nextConfig;

// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   distDir: "out",
// };

// module.exports = nextConfig;

// export default nextConfig;



const nextConfig = {
    distDir: "out",
  };
  
  export default nextConfig;